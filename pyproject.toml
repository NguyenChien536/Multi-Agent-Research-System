[project]
name = "multiagent-research-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NguyenChien536", email = "chiennguyen18125@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.3",
    "tavily-python>=0.8.4",
]

[project.scripts]
multiagent-research-system = "multiagent_research_system:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
